<% provide_all('users/form_fields', 'sign in') %>
<div class='container'>
  <div class='row mt-5'>
    <div class='col-md-7 m-auto'>
        <%= render 'shared/form', object: :session, url: signin_path %>
    </div>
  </div>
</div>
